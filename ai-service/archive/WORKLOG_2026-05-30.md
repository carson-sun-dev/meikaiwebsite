# 工作记录 · 2026-05-30(下午)

> **本次会话目标**:整理 `costumer_service/` 混乱目录,把 AI 服务接入现网站点骨架,关键技术决策落到代码。
> **范围**:**整理 + 接入骨架**(三选一,不含 Sprint 1 ETL / Sprint 2 LangGraph / 真 LLM 联调)。
> **状态**:✅ 全部完成,type-check 通过;AI 推理仍走 mock。

---

## 1. 关键技术决策

| 决策 | 选择 | 理由 |
|---|---|---|
| "全 API 化"边界 | LLM/Embedding/OCR/Vision/Reranker 走 API;**CAD 本地保留**;**Qdrant 本地保留** | CAD 是几何计算非模型推理,本地零成本毫米级精度是 §2.6 简历亮点;Qdrant 自托管 400m 在 2GB 预算内可控,代码已写好,切云收益不抵改动成本 |
| 向量库内存可行性 | 本地 Qdrant + 现有护栏 | mem_limit 合计 1916m(预算 2048m − 内核 200m),贴红线但有 OOMScoreAdj/psutil 熔断/Nginx 限流三重护栏;如要绝对宽松,推荐升 2C4G |
| 目录改名 | `costumer_service` → `ai-service` | 修拼写错误 + 与 docker-compose service 名对齐 + 语义更准(不只是客服) |
| 浮文件 | 删 `apikey.txt`(空) / `question.txt` 留本地并 gitignore | 内容已被 DESIGN §11 吸收;`.env`/`.env.example` 才是 key 的正解 |
| 前端 Markdown 渲染 | 暂用纯文本(Vue 默认转义) | 装 markdown-it + DOMPurify 推到生产前一步,当前 mock 回复纯文本足够 |

---

## 2. 完成清单(6 任务)

| # | 任务 | 关键产物 |
|---|---|---|
| 1 | 改名 + 全引用更新 | `mv` 目录;改 `.gitignore`(8 行)、`docker-compose.yml`(5 处)、`deploy/docker-compose.prod.yml`(2 处)、`DESIGN.md`(12 处) |
| 2 | 清理浮文件 + .gitignore | 删 `apikey.txt`;`question.txt` + `.claude/` 加 ignore |
| 3 | schemas + knowledge 骨架 | `schemas/{residential,office,storefront}_slots.py` Pydantic 模型;`knowledge/personas/*.md` 从 §5.4 拆分;`knowledge/question_bank/*.yaml` 每槽位 3+ 话术;`few_shots/` + `faq_corpus/` 引导 README |
| 4 | 前端 ChatWidget | `src/api/aiChat.ts`(fetch+ReadableStream+SSE 帧解析+credentials:include);`components/ChatWidget/{ChatWidget,ChatPanel}.vue`;`App.vue` 挂载 |
| 5 | Nginx 边缘路由 | `deploy/edge.ssl.conf` 加 `/api/ai/chat`(proxy_buffering off + 5min timeout + limit_conn 2/limit_req 10r/m)、`/api/ai/upload`(20m body)、`/api/ai/`(通用) |
| 6 | docker-compose 校对 | prod compose 补 `LLM_MODE: ${LLM_MODE:-mock}` 显式兜底;`.env` 从 `.env.example` bootstrap |

---

## 3. 文件改动清单

```
新增:
  ai-service/schemas/__init__.py
  ai-service/schemas/residential_slots.py
  ai-service/schemas/office_slots.py
  ai-service/schemas/storefront_slots.py
  ai-service/knowledge/personas/residential.md
  ai-service/knowledge/personas/storefront.md
  ai-service/knowledge/personas/office.md
  ai-service/knowledge/question_bank/residential.yaml
  ai-service/knowledge/question_bank/storefront.yaml
  ai-service/knowledge/question_bank/office.yaml
  ai-service/knowledge/few_shots/README.md
  ai-service/knowledge/few_shots/{residential,office,storefront}/  (空目录)
  ai-service/knowledge/faq_corpus/README.md
  ai-service/.env                                    (从 .env.example 拷)
  frontend/src/api/aiChat.ts
  frontend/src/components/ChatWidget/ChatWidget.vue
  frontend/src/components/ChatWidget/ChatPanel.vue
  frontend/src/components/ChatWidget/index.ts

改:
  .gitignore                            (改路径 + 加 question.txt/.claude)
  docker-compose.yml                    (5 处路径)
  deploy/docker-compose.prod.yml        (路径 + LLM_MODE 兜底)
  deploy/edge.ssl.conf                  (3 location + 2 limit_zone)
  frontend/src/App.vue                  (+import +<ChatWidget/>)
  ai-service/DESIGN.md                  (12 处 costumer_service → ai-service)

删:
  ai-service/apikey.txt                 (空文件)

改名:
  costumer_service/  →  ai-service/
```

---

## 4. 已知未做 / 下次起步

### 还在 mock 状态

- AI 推理:`LLM_MODE=mock`,需要切到 DeepSeek-V3 + Doubao-Lite(用户开通账号后写入 `ai-service/apikey.txt` 或 `.env`)
- Embedding:`MockEmbedding`(SHA256 伪向量),需要切到 Doubao-embedding
- 没有 LangGraph 编排,没有三 SubGraph,没有报价工具

### 还没接通的端到端

- 前端 vite dev server 的 `/api/ai/*` proxy 配置(`vite.config.ts` 需加 proxy,或者依赖 nginx-proxy 路由——本机调试两种方案二选一)
- backend 的 `migrate.ts` 三张新表还没被实际 apply(改动在 git status M 里,需要起 backend 触发 migration)
- 前端 ChatWidget 没在浏览器中实际跑通(type-check 过但没启 dev server 联调)

### 生产前必补

- `markdown-it` + `dompurify` + `@types/markdown-it` 装上后改 ChatPanel 用 `v-html` + sanitize
- Session cookie:服务端 `Set-Cookie ai_sid=...; HttpOnly; Secure; SameSite=Strict; Max-Age=2592000`(DESIGN §9.2.5 / §11.3)
- ai-service 起步加 `app/main.py` 的 CSRF middleware(校验 Origin)
- LangGraph MySQL Checkpointer 接入(`langgraph-checkpoint-mysql` 已在 pyproject.toml,等 LangGraph 进来一起接)

### 下一阶段建议路径

按工作量 + 风险递增:

1. **半天**:切真 LLM key 联调(Sprint 1 → Sprint 3 局部:DeepSeek-V3 + Doubao-Lite + Doubao-embedding,前端能看到真回复)
2. **1-2 天**:ETL 跑 23 份 xls + Qdrant 灌库 + Hybrid Search 验证(Sprint 1 主体)
3. **2-3 天**:LangGraph Router + 三 SubGraph + 工具集(estimate_storefront_quote 优先,§5.4.2 路径)
4. **0.5 天**:Guardrail 输入输出双向护栏(§3.7 / §10 风险矩阵)
5. **0.5 天**:LangFuse trace + 30 条评测集

---

## 5. 验证命令(下次起步时跑)

```bash
# AI 服务(mock)
cd /Users/carrrson/developer/meikai_website
docker compose --profile ai up ai-service qdrant
curl http://localhost:8000/healthz
curl -N -X POST http://localhost:8000/api/ai/chat \
  -H 'Content-Type: application/json' -d '{"message":"测试"}'

# 前端
cd frontend && pnpm dev
# 浏览器右下角红色 FAB → 打开 → 输入消息 → 应看到 mock 流式回复
```

如果前端 dev 看不到回复,大概率是 vite proxy 没配——临时方案:在 `vite.config.ts` 加:
```ts
server: {
  proxy: { '/api/ai': { target: 'http://127.0.0.1:8000', changeOrigin: true } }
}
```
